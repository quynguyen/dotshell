# Notes to Quy
As of Mac 10.12+, you use a script
./bin/mac-remap-right-alt-as-control

#Install Homebrew
/usr/bin/ruby -e "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/master/install)"

#Brew Install
# This lis below is produced with the commannd
brew install $(cat ./homebrew-list)

#Gem
gem install tmuxinator
